// Main application entry point - loads all modules
// This file replaces the monolithic app.js with modular imports

// Load all modules in order
// Core functionality must be loaded first
// Then feature modules can be loaded

// Note: In a real deployment, you would use a module bundler like webpack or vite
// For now, we'll assume all modules are loaded via script tags in the HTML

console.log('IonXE Control System - Modular Architecture Loaded');
