OS Architecture Overview (Draft)

Kernel
- Memory management, scheduling, IPC, syscalls.

Drivers & HAL
- Clear contracts for device classes (HID, display, storage, network, audio).

Userland
- Services, CLI, desk UI, configuration.

Non-goals
- No compatibility layer for Windows. This is a standalone OS.

