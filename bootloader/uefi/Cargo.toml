[package]
name = "ionxe-boot-uefi"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = { version = "0.28", features = ["alloc"] }
uefi-services = "0.26"
log = "0.4"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = "thin"


