[package]
name = "ionxe-boot-uefi"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = { version = "0.28", features = ["alloc"] }
log = "0.4"
xmas-elf = "0.9"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = "thin"


