Bootloader

Purpose
- Hardware bring-up, memory map setup, transition to kernel entry.

Notes
- Investigate coreboot + Tianocore vs custom stage-2 loader.
- Document handoff contract in `specs/boot_handoff.md`.

